\section{Question 1: Representation Learning in Text Classification}

Question 1 write-up is pending the larger-budget BiLSTM and DistilBERT experiments. The current smoke-test comparison and preprocessing summary artifacts are already available and can be merged into this section once the final model comparison is stable.